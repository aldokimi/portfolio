{
  "name": "portfolio",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "forwardPorts": [3000],
  "postCreateCommand": "yarn install && yarn cf-typegen && yarn d1:migrate:local",
  "postStartCommand": "yarn cf-typegen"
}
